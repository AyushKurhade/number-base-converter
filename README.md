# number-base-converter
A simple C++ console application that lets users convert numbers between binary, decimal, hexadecimal, and octal formats. The program validates input strictly for the chosen number base and prompts the user again if invalid characters are entered.
